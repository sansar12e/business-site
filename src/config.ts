/**
 * Site Configuration
 * Edit src/config.json to customize colors, content, and branding
 * This file imports the JSON config so DecapCMS can edit it
 */

import configData from './config.json';

export const siteConfig = configData;
